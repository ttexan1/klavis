# GitHub Authentication Token
# Required for authenticated API requests
GITHUB_AUTH_TOKEN=your_github_personal_access_token

# Server Configuration
# Port where the SSE server will listen (defaults to 5000 if not set)
PORT=5000

# Base URL for the server (defaults to http://localhost:PORT if not set)
BASE_URL=http://localhost:5000